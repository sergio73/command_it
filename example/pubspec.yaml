name: flutter_weather_demo
publish_to: none
description: Demo project for command_it .
version: 1.0.0+1
environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  command_it: 
    path: ../
  listen_it: ^5.0.0

dev_dependencies:
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
